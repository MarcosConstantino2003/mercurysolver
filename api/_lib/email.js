export async function sendVerificationCode(email, code) {
  console.log(`\n================================`);
  console.log(`[MOCK EMAIL] To: ${email} | Subject: Código de verificación de MercurySolver`);
  console.log(`[MOCK EMAIL] Body: Tu código de acceso es ${code}`);
  console.log(`================================\n`);
}
